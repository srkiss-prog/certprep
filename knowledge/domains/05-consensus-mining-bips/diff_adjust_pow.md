# Overview

Difficulty adjustment keeps block production near target despite changing hash rate. Proof-of-work secures ordering by making block creation expensive and probabilistic. If hash rate rises, blocks come faster until retarget; if it falls, blocks slow until retarget. Consensus follows cumulative work, not naive block count. confirmations increase confidence probabilistically because reversing deeper history requires redoing and overtaking more work.
